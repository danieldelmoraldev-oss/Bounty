# Cargado automáticamente por Expo/EAS cuando NODE_ENV=production (builds de
# preview/production). Sustituir por la URL real del servicio de Render en
# cuanto exista.
EXPO_PUBLIC_API_URL=https://bounty-server.onrender.com
